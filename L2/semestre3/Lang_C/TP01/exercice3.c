#include <stdio.h>

int main(void)
{
	int a,b;
	a=16; //variable ecrit en décimal
	b=016; //variable ecrit en octal
	printf("Pourquoi la variable a (=%d) a une valeur différente de la variable b (=%d) ? \n", a, b);
	return(0);
}
